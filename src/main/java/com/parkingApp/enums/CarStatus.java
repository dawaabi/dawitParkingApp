package com.parkingApp.enums;

public enum CarStatus {
    PARKED,      // Car is currently parked

    RESERVED,    // Car has a reserved slot
    WAITING,     // Car is waiting for a slot
    EXITED       // Car has left the parking area
}
